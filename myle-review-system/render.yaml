services:
  - type: web
    name: myle-review-system
    runtime: python
    rootDir: myle-review-system
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
